{
  "name": "11a3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/whiteboard",
    "build": "npm run build --workspace apps/whiteboard",
    "preview": "npm run preview --workspace apps/whiteboard",
    "typecheck": "tsc --noEmit -p apps/whiteboard"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
